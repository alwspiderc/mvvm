/**
 * @typedef {Object} LoginViewModel
 * @property {string} email - The email of the user.
 * @property {string} password - The password of the user.
 * @property {function} setEmail - Function to set the email.
 * @property {function} setPassword - Function to set the password.
 * @property {boolean} isLoading - Indicates if the login is in progress.
 * @property {function} onSubmit - Function to submit the login form.
 */

/**
 * @returns {LoginViewModel} The login view model.
 */
